/**
 * Phase 1 placeholder shell.
 *
 * This is NOT the Welcome screen or the app shell required by Design Freeze
 * v1 / Journey Design Specification — those are built in Phase 4 against the
 * approved visual reference. This component exists only so the frontend
 * build/deploy pipeline has something real to compile, test, and ship to all
 * three environments before any product screen exists.
 */
export function App() {
  return (
    <main data-testid="phase1-placeholder">
      <p>beeside First Assessment — build pipeline online (Phase 1 skeleton).</p>
    </main>
  );
}
